server:
  port: 8081

spring:
  cloud:
    config:
      server:
        git:
          uri: https://github.com/warriors230/microservices.git
          clone-on-start: true
          searchPaths: config-data
          username: ${GIT_USER}
          password: ${GIT_PASS}
          default-label: main
  security:
    user:
      name: root
      password: s3cr3t  