server:
  port: 8081

spring:
  cloud:
    config:
      server:
        bootstrap: true
        git:
          uri: https://github.com/warriors230/microservices.git
          searchPaths: config-data
          username: ${GIT_USER}
          password: ${GIT_PASS}
          default-label: main
  security:
    user:
      name: root
      password: s3cr3t  